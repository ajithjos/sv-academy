# SystemVerilog Academy Context

- Runtime: public Next.js standalone container.
- Local dev: `make setup && source dev/sourceme && make dev-up`.
- Validation: `make check`.
- Deploy: `make deploy-plan && make deploy`.
- Deploy guardrail: the tracked GCP config, project, account, and token must match before Cloud Run deploys run.
